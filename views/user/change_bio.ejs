<% layout('partials/navbar')%>

<div class="columns">
    <%- include('../partials/aside') %>
      
      
        <form class="column mt-6 is-offset-one-fifth is-one-quarter" action="/users/edit/bio/<%=user._id%>?_method=PUT" method="POST">
            <div class="field">
                <h1 class="title is-4">Change your bio</h1>
            </div>

            <div class="field">
                <p class="control has-icons-left">
                    <textarea class="textarea" maxlength="300" placeholder="Maximum 300 characters" name="bio" cols="50" rows="5"></textarea>
                </p>
            </div>
                  
            <div class="field">
                <p class="control">
                        <button type="submit" class="button is-success">
                            Change
                        </button>
                </p>
            </div>
            <a href="/users/settings/<%=user._id%>">Discard</a>
        </form>
    
</div>